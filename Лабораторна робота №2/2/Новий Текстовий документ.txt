#include <stdio.h>

int main() {
    // Задані сталі величини
    int a = 100, b = 0, c = -2;
    // Обчислення значення функції
    double y = 4 - (3 * (a + b + c) / (2 - c))-(1 / b);
    // Виведення результату
    printf("\n Значення функції y =  %.3f\n",y);
    return 0;
}
